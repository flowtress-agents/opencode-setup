[meta]
name = "sandbox"
version = "0.1.0"
description = "Docker sandbox container with herdr + piCode pre-installed and bash terminal on startup"

[layout]
tools = ["tools/herdr.toml", "tools/picode.toml"]
runtime = ["runtime/docker.toml", "runtime/system.toml"]
limits = "limits.toml"
scripts = ["scripts/launch-sandbox.toml"]
